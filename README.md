# juniper

time tracking webapp for analyzing productivity habits

- install as pwa
- entirely private + local (relies on browser storage, so no server needed)
- import and export data with csv format
- list, calendar, and graph views
